# Auto-detected via Expo bundler host — no need to set this manually for local dev.
# Only set this if you're pointing to a deployed backend (e.g. production server).
# EXPO_PUBLIC_API_URL=https://your-production-server.com
