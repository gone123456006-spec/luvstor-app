/**
 * Explore now uses the same CallOverlay as chat (Anonymous + Skip).
 * Kept as a no-op mount so older imports / layout stay safe.
 */
export default function ExploreCallOverlay() {
  return null;
}
